package com.blackspider.recetapp.model.pk

import com.blackspider.recetapp.model.mMedicamento
import com.blackspider.recetapp.model.mReceta

data class mPkRecetaDetalle (var mreceta : mReceta?, var mmedicamento : mMedicamento?)